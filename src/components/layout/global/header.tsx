import { View, Text, Image } from "react-native";

import { Ionicons } from "@expo/vector-icons";

const Header = () => {
	return (
		<View
			style={{
				display: "flex",
			}}>
			<Text>Logo</Text>
		</View>
	);
};

export default Header;
